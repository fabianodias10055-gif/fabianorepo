[phases.setup]
aaptPkgs = ["python3", "python3-pip", "python3-venv"]

[phases.install]
cmds = [
  "python3 -m venv /opt/venv",
  "/opt/venv/bin/pip install -r discord-feedback-bot/requirements.txt"
]

[start]
cmd = "/opt/venv/bin/python3 discord-feedback-bot/bot.py"
