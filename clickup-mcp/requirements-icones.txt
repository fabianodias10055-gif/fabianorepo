# Só é necessário para regenerar os ícones com gerar_icones.py.
# Separado do requirements.txt principal porque o servidor MCP funciona sem
# ele (os PNGs já vêm prontos em assets/icones/).
#
# O renderizador é svglib + reportlab com backend pycairo (tudo com wheel para
# Windows). O cairosvg foi abandonado porque depende da DLL nativa do Cairo,
# que não existe no Windows. Atenção: não instale cairosvg/cairocffi junto —
# o rlPyCairo prefere o cairocffi quando presente e volta a exigir a DLL.
svglib>=1.5,<2.0
reportlab>=4.0,<5.0
pycairo>=1.26,<2.0
rlPyCairo>=0.3,<1.0
Pillow>=11.0,<12.0
